# 生物医药数字信息系统接口设计规范与接口清单

> 版本：V1.2｜日期：2026-07-16｜RESTful 风格｜适用范围：后端开发、前后端联调、测试与文档审查
> 本次更新：同步冻结版已落地模块入口、Refresh Token 会话续期约定与专项文档优先级。

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 生物医药数字信息系统 |
| 接口风格 | RESTful API |
| 数据格式 | JSON；文件上传使用 multipart/form-data |
| 认证方式 | Authorization: Bearer <token> |
| 接口前缀 | /api |
| 说明 | 本文档给出接口设计规范和资源化接口清单，避免动作式 URL、方法滥用、状态码混乱和响应格式不统一。 |

## 0. 文档状态与接口依据

本文档第 1 至 4 节是必须遵守的接口规范；第 5 节是全业务资源的目标设计清单，其中未实现模块不代表当前已经提供对应接口。当前可调用接口依次以以下内容为准：

1. 后端 `/v3/api-docs` 和 Controller 源码。
2. 已落地模块的专项接口文档。
3. 本文档第 5 节的规划清单。

当前已经落地的主要入口包括：

| 业务范围 | 当前入口 |
| --- | --- |
| 认证与权限 | `/api/auth`、`/api/users`、`/api/roles`、`/api/permissions`、`/api/menus`、`/api/me` |
| 文件与审计 | `/api/files`、`/api/public-files`、`/api/file-relations`、`/api/audit-logs`、`/api/login-logs`、`/api/file-access-logs`、`/api/data-sync-logs` |
| 中药材与识别 | `/api/herb/species`、`/api/herb/image`、`/api/herb/atlas`、`/api/herb/identification` |
| 采集与移动端 | `/api/herb/collection-task`、`/api/herb/batch`、`/api/mobile/herb` |
| 基础数据与教学科研 | `/api/dictionaries`、`/api/regions`、`/api/herb-bases`、`/api/growth-records`、`/api/courses`、`/api/research-projects`、`/api/training-plans` |
| 评价申报与业绩认定 | `/api/evaluation-tasks`、`/api/evaluation-standards`、`/api/declarations`、`/api/performances`、`/api/performance-standards`、`/api/performance-statistics` |
| 地图、SOAP、看板 | `/api/map-points`、`/api/soap-exchange-jobs`、`/api/soap-field-mappings`、`/api/dashboard` |

中药材识别以 [04_中药材图像识别接口说明_V1.0_全组_20260710.md](04_中药材图像识别接口说明_V1.0_全组_20260710.md) 为准，采集任务与批次以 [04_采集任务与批次档案接口说明_V1.0_全组_20260710.md](04_采集任务与批次档案接口说明_V1.0_全组_20260710.md) 为准。鉴权基础接入以 [04_用户鉴权模块后端接口接入说明_V1.1_全组_20260716.md](04_用户鉴权模块后端接口接入说明_V1.1_全组_20260716.md) 为准，令牌续期以 [04_鉴权会话续期接口说明_V1.0_全组_20260716.md](04_鉴权会话续期接口说明_V1.0_全组_20260716.md) 为准；业绩和 SOAP 演示以各自专项说明为准。

## 1. 接口设计总体原则

- 接口路径以资源为中心，使用名词表达资源，不在路径中使用 get、add、update、delete、query、list、save、do、handle 等动作词。

- 资源集合统一使用复数形式，例如 users、roles、herbs、courses、files。

- 使用 HTTP 方法表达操作语义：GET 查询，POST 创建资源或提交复杂业务记录，PUT 整体更新，PATCH 局部更新，DELETE 删除。

- 分页、排序、关键词、状态、时间范围和筛选条件统一放在 Query 参数中。

- 新增、更新和局部修改接口使用 JSON 请求体；文件上传使用 multipart/form-data。

- 路径层级原则上不超过三层，复杂查询通过 Query 参数表达，避免路径过深。

- 业务状态流转尽量资源化表达，如提交记录、审核记录、归档记录、确认记录和重试记录。

- 除登录、刷新会话、初始化和明确公开的接口外，所有受保护接口均要求携带 Bearer Token，并进行角色权限和数据范围校验。

## 2. 通用请求规范

### 2.1 Header 规范

| Header 名称 | 是否必填 | 说明 |
| --- | --- | --- |
| Authorization | 受保护接口必填 | 格式为 Bearer <token>，用于身份认证。 |
| Content-Type | 写操作必填 | 普通 JSON 请求使用 application/json；文件上传使用 multipart/form-data。 |
| Accept | 建议填写 | 建议为 application/json。 |

### 2.2 Query 参数规范

| 参数 | 说明 | 示例 |
| --- | --- | --- |
| page | 页码，从 1 开始。 | page=1 |
| size | 每页条数。 | size=10 |
| keyword | 关键词搜索。 | keyword=黄连 |
| sort | 排序字段和方向。 | sort=createTime,desc |
| status | 状态筛选。 | status=SUBMITTED |
| startTime / endTime | 时间范围筛选。 | startTime=2026-07-01&endTime=2026-07-12 |

现有基础模块使用 `page`、`size`；识别、采集和移动端模块当前仍存在 `pageNum`、`pageSize` 兼容输入。为避免破坏已联调客户端，冻结版保留两套现行参数，但禁止继续扩散：

1. 新增通用接口优先使用 `page`、`size`，并复用 `BaseQuery`。
2. 修改现有 `pageNum`、`pageSize` 接口时，应先提供兼容绑定，再经过弃用周期统一。
3. 分页响应始终使用 `page`、`size`、`total`、`records`，不随请求参数名称变化。

分页兼容实现和移除旧参数的后期验收要求见 [03_代码一致性重构需求说明_V1.1_全组_20260716.md](03_代码一致性重构需求说明_V1.1_全组_20260716.md)。

### 2.3 请求体命名规范

- JSON 字段统一使用小驼峰命名，例如 herbId、baseId、collectTime、businessType。

- ID 字段命名统一为 xxxId，例如 userId、roleId、fileId。

- 状态字段使用明确枚举值，例如 DRAFT、SUBMITTED、APPROVED、REJECTED、ARCHIVED。

- 不得将复杂对象、数组、长文本、密码、Token 等内容放入 URL。

## 3. 状态码与响应格式

### 3.1 HTTP 状态码

| 状态码 | 使用场景 |
| --- | --- |
| 200 OK | 查询成功或普通操作成功。 |
| 201 Created | 资源创建成功。 |
| 204 No Content | 删除成功且无返回体。 |
| 400 Bad Request | 请求参数错误、字段校验失败或请求体格式错误。 |
| 401 Unauthorized | 未登录、Token 缺失或 Token 失效。 |
| 403 Forbidden | 已登录但无权限访问该资源或执行该操作。 |
| 404 Not Found | 请求资源不存在。 |
| 409 Conflict | 资源冲突，例如重复账号、重复标准版本、状态冲突。 |
| 500 Internal Server Error | 服务器内部错误。 |

### 3.2 统一响应格式

普通成功响应建议统一返回以下结构。HTTP status 表达传输和协议层结果，业务 code 表达业务层结果，二者不得混用。

```json
{
  "code": "SUCCESS",
  "message": "操作成功",
  "data": {},
  "timestamp": "2026-07-07T10:00:00+08:00"
}
```

### 3.3 分页响应格式

```json
{
  "code": "SUCCESS",
  "message": "查询成功",
  "data": {
    "records": [],
    "page": 1,
    "size": 10,
    "total": 0
  },
  "timestamp": "2026-07-07T10:00:00+08:00"
}
```

### 3.4 错误响应格式

```json
{
  "code": "VALIDATION_ERROR",
  "message": "请求参数错误",
  "data": {
    "field": "name",
    "reason": "名称不能为空"
  },
  "timestamp": "2026-07-07T10:00:00+08:00"
}
```

## 4. 幂等性与安全要求

| 项目 | 设计要求 |
| --- | --- |
| 幂等性 | GET、PUT、DELETE 应具有幂等性；POST 通常不要求幂等；PATCH 按具体字段修改语义设计。 |
| 认证 | 除登录接口外，所有接口均通过 Authorization Header 携带 Bearer Token。 |
| 权限 | 用户、角色、文件、审核、申报、业绩、SOAP 交换等敏感接口必须进行权限控制。 |
| 敏感数据 | URL 中不得暴露密码、Token、身份证号等敏感信息。 |
| 日志 | 新增、修改、删除、审核、下载、归档、SOAP 交换等关键操作应写入操作日志。 |
| 删除策略 | 重要业务数据建议逻辑删除；删除接口成功且无返回体时使用 204 状态码。 |

## 5. RESTful 目标接口清单

本节包含现行接口和后续规划。未在第 0 节或专项文档中列出的路径，应先检查 Controller/OpenAPI，不得仅根据本节假定接口已经实现。

### 5.1 认证与当前用户

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 用户登录 | POST | /api/auth/sessions | Body | 创建登录会话，成功后返回 Token 和用户摘要信息。 |
| 用户退出 | DELETE | /api/auth/sessions/current | Header | 删除当前登录会话或使当前 Token 失效。 |
| 获取当前用户 | GET | /api/auth/me | Header | 根据 Token 返回当前登录用户信息。 |
| 获取当前用户菜单 | GET | /api/me/menus | Header | 返回当前用户可访问菜单。 |
| 获取当前用户按钮权限 | GET | /api/me/buttons | Header, Query | 可通过 menuCode 查询指定菜单下按钮权限。 |

### 5.2 用户、角色与组织

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 查询用户列表 | GET | /api/users | Query, Header | 支持分页、关键词、状态、角色筛选。 |
| 新增用户 | POST | /api/users | Body, Header | 创建用户。 |
| 查询用户详情 | GET | /api/users/{userId} | Path, Header | 查询单个用户。 |
| 整体更新用户 | PUT | /api/users/{userId} | Path, Body, Header | 更新用户完整信息。 |
| 局部更新用户 | PATCH | /api/users/{userId} | Path, Body, Header | 修改用户状态、角色等局部字段。 |
| 删除用户 | DELETE | /api/users/{userId} | Path, Header | 逻辑删除或停用用户。 |
| 查询角色列表 | GET | /api/roles | Query, Header | 支持分页和关键词。 |
| 新增角色 | POST | /api/roles | Body, Header | 创建角色。 |
| 查询角色详情 | GET | /api/roles/{roleId} | Path, Header | 查询角色详情。 |
| 更新角色 | PUT | /api/roles/{roleId} | Path, Body, Header | 更新角色。 |
| 删除角色 | DELETE | /api/roles/{roleId} | Path, Header | 删除角色。 |
| 查询组织列表 | GET | /api/organizations | Query, Header | 可通过 view=tree 返回树形结构。 |
| 新增组织 | POST | /api/organizations | Body, Header | 创建组织。 |
| 更新组织 | PUT | /api/organizations/{orgId} | Path, Body, Header | 更新组织。 |

### 5.3 权限与数据字典

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 权限判定 | POST | /api/authorization-decisions | Body, Header | 请求体传资源类型、资源 ID 和操作类型，返回是否允许。 |
| 查询字典类型 | GET | /api/dictionaries | Query, Header | 查询字典类型列表。 |
| 新增字典类型 | POST | /api/dictionaries | Body, Header | 创建字典类型。 |
| 查询字典项 | GET | /api/dictionaries/{dictType}/items | Path, Query, Header | 根据字典类型查询字典项。 |
| 新增字典项 | POST | /api/dictionaries/{dictType}/items | Path, Body, Header | 新增字典项。 |
| 更新字典项 | PUT | /api/dictionaries/{dictType}/items/{itemId} | Path, Body, Header | 更新字典项。 |
| 删除字典项 | DELETE | /api/dictionaries/{dictType}/items/{itemId} | Path, Header | 删除字典项。 |

已配置且启用的 `herb_category`、`growth_stage`、`soil_type`、`weather`、`course_type`、`course_resource_type` 会被对应业务模块校验；未配置字典类型时保留原有自由文本兼容行为。

### 5.4 文件资源与审计日志

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 上传文件 | POST | /api/files | Body, Header | 使用 multipart/form-data 上传文件并保存元数据。 |
| 查询文件详情 | GET | /api/files/{fileId} | Path, Header | 返回文件元数据。 |
| 获取文件内容 | GET | /api/files/{fileId}/content | Path, Query, Header | 通过 disposition=inline/attachment 控制预览或下载。 |
| 获取公开文件内容 | GET | /api/public-files/{fileId}/content | Path | 仅允许访问 accessLevel=public 的文件，不要求登录。 |
| 查询业务附件 | GET | /api/files | Query, Header | 通过 businessType、businessId 筛选业务附件。 |
| 删除文件 | DELETE | /api/files/{fileId} | Path, Header | 未绑定文件允许上传者或管理员删除；已绑定文件逐一校验解绑权限，公开文件同时校验发布/撤回权限。 |
| 创建文件关联 | POST | /api/file-relations | Body, Header | 关联文件与业务对象。 |
| 删除文件关联 | DELETE | /api/file-relations/{relationId} | Path, Header | 解除文件与业务对象关系。 |
| 查询操作日志 | GET | /api/audit-logs | Query, Header | 支持 operatorId、operationModule、operationType、operationResult、bizType、bizId、startTime、endTime 及分页筛选。 |
| 查询日志详情 | GET | /api/audit-logs/{logId} | Path, Header | 查询单条日志。 |
| 查询登录日志 | GET | /api/login-logs | Query, Header | 支持 userId、username、loginResult、startTime、endTime 及分页筛选。 |
| 查询文件访问日志 | GET | /api/file-access-logs | Query, Header | 支持 fileId、operatorId、accessType、accessResult、startTime、endTime 及分页筛选。 |
| 查询数据同步日志 | GET | /api/data-sync-logs | Query, Header | 支持 syncType、sourceType、targetType、syncStatus、taskId、businessType、businessId、externalNo、startTime、endTime 及分页筛选。 |

四类审计列表均继承公共分页条件 `page`、`size`、`keyword`、`startTime`、`endTime`，其中 `size` 最大为 200。操作、登录、文件访问、数据同步日志分别要求 `audit:operation:view`、`audit:login:view`、`audit:file:view`、`audit:data-sync:view` 权限。操作日志详情当前仅由 `/api/audit-logs/{logId}` 提供。

文件上传只允许使用 `accessLevel=private`。底层 `/api/files/uploads/**` 存储路径由 Security 显式拒绝，服务层也不再解析；业务附件必须通过受保护的内容接口读取。文件完成业务绑定并通过对应模块审核或发布后，由业务模块调用文件服务切换为公开状态，上传接口和批量导入接口都不得直接创建公开文件。只有显式公开的文件可使用 `/api/public-files/{fileId}/content`。地图封面采用“私有上传 → 保存点位 → `map_point` Policy 绑定 → 地图业务发布”的顺序。

### 5.5 药材档案、基地与地图

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 查询药材列表 | GET | /api/herb/species/page | Query, Header | 支持分页、关键词、分类筛选。 |
| 新增药材 | POST | /api/herb/species | Body, Header | 创建药材档案。 |
| 查询药材详情 | GET | /api/herb/species/{herbId} | Path, Header | 查询药材基础信息。 |
| 整体更新药材 | PUT | /api/herb/species/{herbId} | Path, Body, Header | 整体更新药材信息。 |
| 删除药材 | DELETE | /api/herb/species/{herbId} | Path, Header | 逻辑删除药材。 |
| 查询基地列表 | GET | /api/herb-bases | Query, Header | 支持区县、药材、负责人筛选。 |
| 新增基地 | POST | /api/herb-bases | Body, Header | 创建基地。 |
| 查询基地详情 | GET | /api/herb-bases/{baseId} | Path, Header | 查询基地详情。 |
| 更新基地 | PUT | /api/herb-bases/{baseId} | Path, Body, Header | 更新基地。 |
| 查询地图点位 | GET | /api/map-points | Query, Header | 支持 herbId、district、baseId 筛选。 |
| 新增地图点位 | POST | /api/map-points | Body, Header | 创建地图点位。 |
| 更新地图点位 | PUT | /api/map-points/{pointId} | Path, Body, Header | 更新地图点位。 |

### 5.6 生长采集、审核与溯源

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 查询采集记录 | GET | /api/growth-records | Query, Header | 支持分页、药材、基地、采集人、状态、时间筛选。 |
| 新增采集记录 | POST | /api/growth-records | Body, Header | 移动网页版提交采集数据。 |
| 查询采集详情 | GET | /api/growth-records/{recordId} | Path, Header | 查询采集记录详情。 |
| 整体更新采集记录 | PUT | /api/growth-records/{recordId} | Path, Body, Header | 草稿或退回状态下整体更新。 |
| 局部更新采集记录 | PATCH | /api/growth-records/{recordId} | Path, Body, Header | 修改状态或部分字段。 |
| 删除采集记录 | DELETE | /api/growth-records/{recordId} | Path, Header | 逻辑删除采集记录。 |
| 提交采集记录 | POST | /api/growth-records/{recordId}/submissions | Path, Body, Header | 创建提交记录。 |
| 审核采集记录 | POST | /api/growth-records/{recordId}/audit-records | Path, Body, Header | 请求体传 decision 和 comment。 |
| 归档采集记录 | POST | /api/growth-records/{recordId}/archives | Path, Body, Header | 创建归档记录。 |
| 查询采集溯源 | GET | /api/growth-records/{recordId}/trace-events | Path, Header | 查询采集、修改、审核、归档链路。 |
| 上传采集现场图片 | POST | /api/herb/image/upload | Body, Header | 使用 multipart/form-data；传 speciesId、growthRecordId，可选 distributionId，文件同时关联图片与生长记录。 |

采集详情响应包含 `images` 列表；图片通过 `/api/files/{fileId}/content` 受保护接口读取。上传成功后会创建 `sys_file_resource`、`sys_file_business` 与 `herb_image` 的一致关联，并触发图片特征提取。

### 5.7 图谱知识

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 查询图谱文件 | GET | /api/spectra | Query, Header | 支持药材、类型、关键词筛选。 |
| 新增图谱文件 | POST | /api/spectra | Body, Header | 创建图谱记录，文件本体由文件模块管理。 |
| 查询图谱详情 | GET | /api/spectra/{spectrumId} | Path, Header | 查询图谱元数据和关联文件。 |
| 更新图谱记录 | PUT | /api/spectra/{spectrumId} | Path, Body, Header | 更新图谱信息。 |
| 新增图谱比对记录 | POST | /api/spectrum-comparisons | Body, Header | 记录样本和标准图谱的人工比对结果。 |
| 查询图谱比对详情 | GET | /api/spectrum-comparisons/{comparisonId} | Path, Header | 查询比对结果。 |
| 查询药材知识图谱 | GET | /api/herb/species/{herbId}/knowledge-graph | Path, Header | 返回节点和边数据，当前为规划接口。 |

### 5.8 实验课程、课题与实验记录

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 查询课程列表 | GET | /api/courses | Query, Header | 支持分页、关键词、状态筛选。 |
| 新增课程 | POST | /api/courses | Body, Header | 创建课程。 |
| 查询课程详情 | GET | /api/courses/{courseId} | Path, Header | 查询课程。 |
| 更新课程 | PUT | /api/courses/{courseId} | Path, Body, Header | 整体更新课程。 |
| 删除课程 | DELETE | /api/courses/{courseId} | Path, Header | 删除课程并清理步骤、资源记录与文件关联。 |
| 发布课程 | PATCH | /api/courses/{courseId}/publish-status | Path, Query, Header | 通过 status=draft/published/archived 修改发布状态。 |
| 新增实验步骤 | POST | /api/courses/{courseId}/steps | Path, Body, Header | 为课程新增有序实验步骤。 |
| 更新实验步骤 | PUT | /api/courses/{courseId}/steps/{stepId} | Path, Body, Header | 更新课程实验步骤。 |
| 删除实验步骤 | DELETE | /api/courses/{courseId}/steps/{stepId} | Path, Header | 删除课程实验步骤。 |
| 新增课程资源 | POST | /api/courses/{courseId}/resources | Path, Body, Header | 绑定统一文件资源并保存课程资源元数据。 |
| 删除课程资源 | DELETE | /api/courses/{courseId}/resources/{resourceId} | Path, Header | 删除课程资源并解除文件关联。 |
| 查询课题列表 | GET | /api/projects | Query, Header | 支持分页、关键词、负责人筛选。 |
| 新增课题 | POST | /api/projects | Body, Header | 创建课题。 |
| 查询课题详情 | GET | /api/projects/{projectId} | Path, Header | 查询课题详情。 |
| 更新课题 | PUT | /api/projects/{projectId} | Path, Body, Header | 更新课题。 |
| 添加课题成员 | POST | /api/projects/{projectId}/members | Path, Body, Header | 创建课题成员关系。 |
| 查询课题材料 | GET | /api/projects/{projectId}/materials | Path, Query, Header | 查询课题关联材料。 |
| 查询实验记录 | GET | /api/experiment-records | Query, Header | 支持课程、课题、创建人筛选。 |
| 新增实验记录 | POST | /api/experiment-records | Body, Header | 创建实验记录。 |
| 查询实验记录详情 | GET | /api/experiment-records/{recordId} | Path, Header | 查询实验记录。 |
| 更新实验记录 | PUT | /api/experiment-records/{recordId} | Path, Body, Header | 更新实验记录。 |
| 归档实验记录 | POST | /api/experiment-records/{recordId}/archives | Path, Body, Header | 创建归档记录。 |

### 5.9 培训、评价与申报

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 查询培训列表 | GET | /api/trainings | Query, Header | 支持分页、主题、时间筛选。 |
| 新增培训 | POST | /api/trainings | Body, Header | 创建培训活动。 |
| 查询培训详情 | GET | /api/trainings/{trainingId} | Path, Header | 查询培训详情。 |
| 添加培训参与人 | POST | /api/trainings/{trainingId}/participants | Path, Body, Header | 创建参与记录。 |
| 记录培训结果 | POST | /api/trainings/{trainingId}/results | Path, Body, Header | 创建结果记录。 |
| 提交培训反馈 | POST | /api/trainings/{trainingId}/feedback | Path, Body, Header | 创建反馈记录。 |
| 查询评价标准 | GET | /api/evaluation-standards | Query, Header | 支持分页、状态、版本筛选。 |
| 新增评价标准 | POST | /api/evaluation-standards | Body, Header | 创建评价标准。 |
| 更新评价标准 | PUT | /api/evaluation-standards/{standardId} | Path, Body, Header | 更新评价标准。 |
| 查询评价任务 | GET | /api/evaluation-tasks | Query, Header | 支持评价对象、状态筛选。 |
| 新增评价任务 | POST | /api/evaluation-tasks | Body, Header | 创建评价任务。 |
| 查询评价任务详情 | GET | /api/evaluation-tasks/{taskId} | Path, Header | 查询任务详情。 |
| 新增评价记录 | POST | /api/evaluation-records | Body, Header | 提交评分和意见。 |
| 确认评价记录 | POST | /api/evaluation-records/{recordId}/confirmations | Path, Body, Header | 创建确认记录。 |
| 查询申报档案 | GET | /api/declarations | Query, Header | 支持分页、申报类型、状态筛选。 |
| 新增申报档案 | POST | /api/declarations | Body, Header | 创建申报档案袋。 |
| 查询申报档案详情 | GET | /api/declarations/{declarationId} | Path, Header | 查询档案详情。 |
| 添加申报材料 | POST | /api/declarations/{declarationId}/materials | Path, Body, Header | 关联申报材料。 |
| 查询申报汇总 | GET | /api/declarations/{declarationId}/summary | Path, Header | 查询档案汇总信息。 |

### 5.10 业绩认定

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 查询业绩列表 | GET | /api/performances | Query, Header | 支持分页、填报人、类别、状态筛选。 |
| 新增业绩 | POST | /api/performances | Body, Header | 创建业绩记录。 |
| 查询业绩详情 | GET | /api/performances/{performanceId} | Path, Header | 查询业绩详情。 |
| 更新业绩 | PUT | /api/performances/{performanceId} | Path, Body, Header | 更新业绩。 |
| 提交业绩 | POST | /api/performances/{performanceId}/submissions | Path, Body, Header | 创建提交记录。 |
| 审核业绩 | POST | /api/performances/{performanceId}/audit-records | Path, Body, Header | 请求体传 decision 和 comment。 |
| 查询业绩统计 | GET | /api/performance-statistics | Query, Header | 支持维度、时间、人员、类别筛选。 |

### 5.11 SOAP 任务管理（模拟校内系统对接）

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 创建 SOAP 交换任务 | POST | /api/soap-exchange-jobs | Body, Header | 冻结版仅接受 `resourceType=GROWTH_RECORD`；仅本地 mock、入站同步，服务与方法由后端固定。 |
| 查询 SOAP 交换任务 | GET | /api/soap-exchange-jobs | Query, Header | 支持资源类型、状态、时间筛选。 |
| 查询 SOAP 交换详情 | GET | /api/soap-exchange-jobs/{jobId} | Path, Header | 查询请求、响应、解析结果。 |
| 创建重试任务 | POST | /api/soap-exchange-jobs/{jobId}/retries | Path, Body, Header | 对失败任务创建一次重试记录。 |
| 查询字段映射 | GET | /api/soap-field-mappings | Query, Header | 查询字段映射配置。 |
| 新增字段映射 | POST | /api/soap-field-mappings | Body, Header | 创建字段映射。 |
| 更新字段映射 | PUT | /api/soap-field-mappings/{mappingId} | Path, Body, Header | 更新映射配置。 |

冻结版只支持 `resourceType=GROWTH_RECORD`，固定调用 `CampusGrowthDataService/queryGrowthRecords`。响应会校验药材、基地和地图点位后真正写入生长采集表，并通过外部编号保证重复执行幂等；其他资源类型、方向、服务或方法参数会被拒绝，不能被记录为看似成功的 `PREPARED` 任务。

本节 REST 接口用于创建任务和查询交换留痕，不等同于校内系统的 SOAP 协议。冻结版已通过 HTTP 调用本地模拟 SOAP 服务；其 SOAP 契约、WSDL 地址、字段映射、演示数据脚本和验收标准见 [03_SOAP模拟校内系统对接预备方案_V1.0_全组_20260715.md](03_SOAP模拟校内系统对接预备方案_V1.0_全组_20260715.md)。

### 5.12 首页看板

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 查询首页汇总 | GET | /api/dashboard/summary | Header | 查询药材、基地、采集、课程等数量；采集、申报和业绩统计遵循当前用户数据范围。 |
| 查询最近采集 | GET | /api/dashboard/recent-growth-records | Query, Header | 支持 limit 参数，并遵循生长记录数据范围。 |
| 查询待办事项 | GET | /api/dashboard/pending-tasks | Query, Header | 支持类型筛选；生长、申报和业绩待办遵循对应数据范围。 |
| 查询地图概览 | GET | /api/dashboard/map-overview | Header | 返回地图概览统计。 |
| 刷新首页快照 | POST | /api/dashboard/snapshots | Header | 显式刷新当日统计快照，需要 `dashboard:refresh` 权限。 |

## 6. 后续开发约束

- 前端不得直接访问数据库，所有业务数据必须通过后端接口获取。

- 后端 Controller 不直接操作数据库，业务逻辑由 Service 负责，数据库访问由 Mapper 负责。

- 新增接口必须先检查路径是否资源化、方法是否符合语义、参数是否放置合理、响应格式是否统一。

- 新增业务状态流转接口时，应优先建模为记录类子资源，例如 audit-records、submissions、archives。

- 接口文档、前端请求封装和后端实现应保持一致，避免同一资源出现多套命名。

## 附：M16-M18 后端实现补充说明

### 统一鉴权与数据范围约束

M16-M18 接口均要求 JWT 登录，并使用 `auth_permission` 权限点和
`auth_data_scope`/角色默认数据范围进行服务端校验。列表按资源所有人过滤，详情、修改、
提交、评分、审核、确认和归档均使用数据库中读取的资源归属再次判定。

| 模块 | 资源类型 | 权限码 |
| --- | --- | --- |
| M16 | `eval_task` | `evaluation:standard:view`、`evaluation:standard:manage`、`evaluation:task:view`、`evaluation:task:create`、`evaluation:score:create`、`evaluation:result:confirm` |
| M17 | `eval_application` | `declaration:application:view`、`declaration:application:create`、`declaration:application:update`、`declaration:application:submit`、`declaration:application:audit`、`declaration:application:archive` |
| M18 | `perf_record` | `performance:standard:view`、`performance:standard:manage`、`performance:record:view`、`performance:record:create`、`performance:record:update`、`performance:record:submit`、`performance:record:audit` |

申请人、评分人、审核人、确认人、上传人和操作人 ID 统一取当前认证上下文。为兼容旧客户端，相关请求体字段可以继续传入，但后端不会采信。

申报和业绩创建状态固定为 `draft`；后续状态只能通过提交、审核和确认等专用流程接口变更。

### M16 评价管理补充

列表类接口已支持分页参数：

| 参数 | 位置 | 说明 |
| --- | --- | --- |
| pageNum | Query | 当前页，默认 1 |
| pageSize | Query | 每页条数，默认 10 |

适用接口：

| 功能 | HTTP 方法 | RESTful 路径 | 说明 |
| --- | --- | --- | --- |
| 查询评价标准 | GET | /api/evaluation-standards | 支持分页、关键字、指标类型、状态筛选。 |
| 查询评价任务 | GET | /api/evaluation-tasks | 支持分页、评价对象、状态筛选。 |

### M17 申报档案补充

列表类接口已支持 pageNum、pageSize 分页参数。

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 提交申报档案 | POST | /api/declarations/{declarationId}/submissions | Path, Body, Header | 将草稿或退回状态的申报提交审核。 |
| 审核申报档案 | POST | /api/declarations/{declarationId}/reviews | Path, Body, Header | 审核通过或退回；审核通过后自动生成档案袋。 |
| 生成申报档案袋 | POST | /api/declarations/{declarationId}/archives | Path, Header | 手动生成或重新同步申报档案袋。 |
| 添加档案材料项 | POST | /api/declaration-archives/{archiveId}/items | Path, Body, Header | 向档案袋追加药材、评价结果、文件等来源材料项。 |

### M18 业绩认定补充

列表类接口已支持分页参数：

| 参数 | 位置 | 说明 |
| --- | --- | --- |
| pageNum | Query | 当前页，默认 1 |
| pageSize | Query | 每页条数，默认 10 |

补充和修订后的接口如下：

| 功能 | HTTP 方法 | RESTful 路径 | 请求参数位置 | 说明 |
| --- | --- | --- | --- | --- |
| 查询业绩列表 | GET | /api/performances | Query, Header | 支持分页、填报人、类别、状态、来源筛选。 |
| 查询认定标准 | GET | /api/performance-standards | Query, Header | 支持分页、关键字、业绩类型、状态筛选。 |
| 添加业绩材料 | POST | /api/performances/{performanceId}/materials | Path, Body, Header | 通过 sys_file_business 关联业绩证明材料。 |
| 查询业绩材料 | GET | /api/performances/{performanceId}/materials | Path, Header | 查询业绩已关联材料清单。 |
| 删除业绩材料 | DELETE | /api/performances/{performanceId}/materials/{relationId} | Path, Header | 仅草稿或退回状态允许解绑材料。 |
| 查询/维护参与人 | GET/POST | /api/performances/{performanceId}/participants | Path, Body, Header | 参与人不能重复，负责人由业绩归属人固定维护。 |
| 修改/移除参与人 | PUT/DELETE | /api/performances/{performanceId}/participants/{participantId} | Path, Body, Header | 仅草稿或退回状态允许维护，不能移除负责人。 |
| 审核业绩 | POST | /api/performances/{performanceId}/audit-records | Path, Body, Header | 支持 decision/comment，也兼容 identifyResult/identifyComment。 |
| 创建标准版本 | POST | /api/performance-standards/{standardId}/versions | Path, Body, Header | 从已有标准创建新的草稿版本。 |
| 发布/停用标准 | POST | /api/performance-standards/{standardId}/publish、/disable | Path, Header | 发布后禁止原地修改；停用不影响已冻结业绩。 |

审核业绩请求体字段兼容说明：

| 字段 | 是否必填 | 说明 |
| --- | --- | --- |
| identifierId | 否 | 兼容字段；后端不采信，认定人始终取当前 JWT 用户。 |
| decision | 否 | 文档标准字段，值为 approved 或 rejected。 |
| comment | 否 | 文档标准字段，认定意见。 |
| identifyResult | 否 | 兼容字段，值为 approved 或 rejected。 |
| identifyComment | 否 | 兼容字段，认定意见。 |
| identifyAction | 否 | 兼容字段；未传时按结果自动推断为 approve/reject。 |

退回时 `comment` 或 `identifyComment` 必填；审核人不能审核本人业绩。业绩提交只接受已发布且在适用期内的标准，并按标准版本校验材料最低数量。
